<h1> Hello Welcome to Hospital Health care </h1>

<% if current_user %>
  <p> Hello <%= current_user.email %>!</p>
     <p> You are a [<%= current_user.role %>]
    <% if current_user.admin? %>
    There are <%= User.count %> User In the system.
    <% end %>

  <p>
  You are now ready to interact with this application
  </p>

  <%= link_to 'sign out', destroy_user_session_path, 'data_turbo_method': :delete %>
  <% else %>
  <p>You are not sing in.</p>
  <%= link_to 'Sign up!', new_user_registration_path %>
  <br/>
  <%= link_to 'Login', new_user_registration_path %>
<% end %>